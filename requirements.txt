# ============================================================================
# 记忆引擎 (Memory Engine) — 核心依赖
# ============================================================================
# Python >= 3.10

# MCP 协议框架
fastmcp

# SSE 传输（HTTP 服务器）
uvicorn

# 向量存储 + 嵌入模型
# FAISS 向量索引（CPU 版本）
faiss-cpu

# ONNX 运行时（嵌入模型推理）
onnxruntime

# ChromaDB（旧版兼容，新部署不使用）
# chromadb

# HTTP 客户端（DeepSeek API 调用、飞书集成）
httpx

# 环境变量管理（.env 文件加载）
python-dotenv

# ============================================================================
# 开发 / 测试依赖（可选）
# ============================================================================
# 安装方式: pip install -r requirements.txt[dev 暂不自动安装]
# 手动安装: pip install pytest pytest-asyncio

# pytest
# pytest-asyncio
