# ============================================================================
# 记忆引擎 (Memory Engine) — 核心依赖
# ============================================================================
# Python >= 3.10

# MCP 协议框架
fastmcp

# SSE 传输（HTTP 服务器）
uvicorn

# 向量存储 + 嵌入模型
faiss-cpu                # FAISS 向量索引（CPU 版本）
sentence-transformers     # 嵌入模型（all-MiniLM-L6-v2）
numpy                     # 向量计算
cachetools                # TTL 内存缓存

# ONNX 运行时（嵌入模型推理）
onnxruntime

# HTTP 客户端（DeepSeek API 调用、飞书集成）
httpx

# 环境变量管理（.env 文件加载）
python-dotenv

# ============================================================================
# 开发 / 测试依赖（可选）
# ============================================================================
# 安装方式: pip install pytest pytest-asyncio
# pytest
# pytest-asyncio
